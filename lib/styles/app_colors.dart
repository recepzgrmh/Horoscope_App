import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(
    0xFFA89E9E,
  ); // Dumanlı gümüş (başlıklar için)
  static const Color secondaryColor = Color(
    0xFF6A5C79,
  ); // Gölge moru (mistik atmosfer)
  static const Color backgroundColor = Color(
    0xFF0D0A0B,
  ); // Gece siyahı (koyu arkaplan)
  static const Color cardColor = Color(
    0xFF1C1A22,
  ); // Kurşun grisi (kartlar için)
  static const Color accentColor = Color(
    0xFF7B1E1E,
  ); // Kan kırmızısı (mistik vurgular)
  static const Color textPrimary = Color(
    0xFFE5E5E5,
  ); // Buz beyazı (temel metinler)
  static const Color textSecondary = Color(
    0xFFB5AEB8,
  ); // Sis grisi (yardımcı metinler)
  static const Color borderColor = Color(
    0xFF4A3A58,
  ); // Derin ametist (kenar çizgileri)
}
