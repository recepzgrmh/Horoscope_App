import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(
    0xFFB0A8B9,
  ); // Dumanlı gümüş (Biraz daha açık)
  static const Color secondaryColor = Color(
    0xFF7C5C91,
  ); // Derin menekşe (Gizemli bir his)
  static const Color backgroundColor = Color(
    0xFF121016,
  ); // Gece moru (Biraz daha sıcak siyah)
  static const Color cardColor = Color(
    0xFF201D2B,
  ); // Koyu ametist (Kartlar için mistik hava)
  static const Color accentColor = Color(
    0xFFD7263D,
  ); // Derin kırmızı (Gizemli ve çarpıcı)
  static const Color textPrimary = Color(
    0xFFF5F5F5,
  ); // Saf beyaz (Daha iyi okunabilirlik)
  static const Color textSecondary = Color(
    0xFFB8A8C9,
  ); // Sis moru (Alt metinler için)
  static const Color borderColor = Color(
    0xFF5A496E,
  ); // Koyu lavanta (Hafif bir gölge)
}
